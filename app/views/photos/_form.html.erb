<%= simple_form_for [@product,@photo] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :product_id %>
    <%= f.input :images %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
